import React from 'react';
import classNames from 'classnames';

const ProjectsContainer = () => (
  <div className={classNames('projects-container')}>ProjectsContainer</div>
);

export default ProjectsContainer;
